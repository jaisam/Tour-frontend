import { MonthYearFormatPipe } from './month-year-format.pipe';

describe('MonthYearFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new MonthYearFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
